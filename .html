<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Arepadle Pro - Aventura por Niveles</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            color: white;
        }

        .header {
            text-align: center;
            margin: 20px 0;
            animation: glow 2s ease-in-out infinite alternate;
        }

        @keyframes glow {
            from { text-shadow: 0 0 20px #fff, 0 0 30px #fff, 0 0 40px #0ff; }
            to { text-shadow: 0 0 10px #fff, 0 0 20px #fff, 0 0 30px #0ff; }
        }

        .level-info {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 15px 30px;
            border-radius: 15px;
            margin: 10px;
            text-align: center;
            border: 2px solid rgba(255, 255, 255, 0.2);
        }

        .progress-bar {
            width: 300px;
            height: 20px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 10px;
            margin: 20px auto;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #00d4aa, #00a8ff);
            border-radius: 10px;
            transition: width 0.5s ease;
            width: 0%;
        }

        .game-container {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(15px);
            padding: 30px;
            border-radius: 20px;
            margin: 20px;
            box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
            border: 1px solid rgba(255, 255, 255, 0.18);
            max-width: 500px;
            width: 100%;
        }

        .word-grid {
            display: flex;
            flex-direction: column;
            gap: 8px;
            margin: 20px 0;
            justify-content: center;
            align-items: center;
        }

        .word-row {
            display: flex;
            gap: 8px;
            justify-content: center;
        }

        .letter-box {
            width: 50px;
            height: 50px;
            border: 2px solid rgba(255, 255, 255, 0.5);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            font-weight: bold;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            transition: all 0.3s ease;
            text-transform: uppercase;
        }

        .letter-box.filled {
            background: rgba(255, 255, 255, 0.3);
            border-color: rgba(255, 255, 255, 0.7);
        }

        .letter-box.correct {
            background: #2ecc71;
            border-color: #27ae60;
            animation: bounce 0.5s ease;
        }

        .letter-box.partial {
            background: #f39c12;
            border-color: #e67e22;
            animation: shake 0.5s ease;
        }

        .letter-box.incorrect {
            background: #e74c3c;
            border-color: #c0392b;
            animation: fadeIn 0.5s ease;
        }

        @keyframes bounce {
            0%, 20%, 60%, 100% { transform: translateY(0); }
            40% { transform: translateY(-10px); }
            80% { transform: translateY(-5px); }
        }

        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            25% { transform: translateX(-5px); }
            75% { transform: translateX(5px); }
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.8); }
            to { opacity: 1; transform: scale(1); }
        }

        .keyboard {
            display: grid;
            grid-template-columns: repeat(10, 1fr);
            gap: 5px;
            margin: 20px 0;
            max-width: 500px;
        }

        .key {
            padding: 12px 8px;
            background: rgba(255, 255, 255, 0.2);
            border: none;
            border-radius: 8px;
            color: white;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 14px;
        }

        .key:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
        }

        .key.correct {
            background: #2ecc71;
        }

        .key.partial {
            background: #f39c12;
        }

        .key.incorrect {
            background: #e74c3c;
        }

        .key.enter {
            grid-column: span 2;
            background: #9b59b6;
        }

        .key.backspace {
            grid-column: span 2;
            background: #e67e22;
        }

        .hint-container {
            background: rgba(255, 255, 255, 0.1);
            padding: 15px;
            border-radius: 10px;
            margin: 15px 0;
            text-align: center;
            border-left: 4px solid #f39c12;
        }

        .controls {
            display: flex;
            gap: 10px;
            justify-content: center;
            margin: 20px 0;
        }

        .btn {
            padding: 12px 24px;
            border: none;
            border-radius: 25px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .btn-primary {
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
        }

        .btn-success {
            background: linear-gradient(45deg, #2ecc71, #27ae60);
            color: white;
        }

        .btn-warning {
            background: linear-gradient(45deg, #f39c12, #e67e22);
            color: white;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.2);
        }

        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0,0,0,0.8);
            backdrop-filter: blur(5px);
        }

        .modal-content {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            margin: 10% auto;
            padding: 30px;
            border-radius: 20px;
            width: 90%;
            max-width: 500px;
            text-align: center;
            color: white;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
        }

        .stars {
            font-size: 2em;
            margin: 20px 0;
        }

        .level-complete {
            background: linear-gradient(45deg, #2ecc71, #27ae60);
            padding: 20px;
            border-radius: 15px;
            margin: 20px 0;
        }

        .next-level-preview {
            background: rgba(255, 255, 255, 0.1);
            padding: 15px;
            border-radius: 10px;
            margin: 15px 0;
        }

        .achievements {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin: 20px 0;
        }

        .achievement {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            background: rgba(255, 255, 255, 0.2);
            border: 2px solid rgba(255, 255, 255, 0.3);
        }

        .achievement.unlocked {
            background: linear-gradient(45deg, #f39c12, #e67e22);
            border-color: #d35400;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { box-shadow: 0 0 0 0 rgba(243, 156, 18, 0.7); }
            70% { box-shadow: 0 0 0 10px rgba(243, 156, 18, 0); }
            100% { box-shadow: 0 0 0 0 rgba(243, 156, 18, 0); }
        }

        .region-badge {
            display: inline-block;
            padding: 5px 15px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            font-size: 12px;
            margin: 5px;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .region-badge.paisa { background: linear-gradient(45deg, #e74c3c, #c0392b); }
        .region-badge.costeno { background: linear-gradient(45deg, #3498db, #2980b9); }
        .region-badge.bogotano { background: linear-gradient(45deg, #9b59b6, #8e44ad); }
        .region-badge.caleno { background: linear-gradient(45deg, #2ecc71, #27ae60); }

        @media (max-width: 600px) {
            .game-container {
                margin: 10px;
                padding: 20px;
            }
            
            .keyboard {
                grid-template-columns: repeat(8, 1fr);
                gap: 3px;
            }
            
            .letter-box {
                width: 40px;
                height: 40px;
                font-size: 16px;
            }
            
            .key {
                padding: 10px 6px;
                font-size: 12px;
                min-height: 40px;
            }
        }

        /* Estilos específicos para iOS */
        @supports (-webkit-touch-callout: none) {
            .key {
                -webkit-tap-highlight-color: transparent;
                -webkit-user-select: none;
                user-select: none;
            }
            
            .letter-box {
                -webkit-tap-highlight-color: transparent;
            }
            
            body {
                -webkit-touch-callout: none;
                -webkit-user-select: none;
                user-select: none;
            }
        }

        /* Prevenir zoom en iOS */
        @media screen and (max-width: 768px) {
            input, select, textarea, button {
                font-size: 16px;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🇨🇴 AREPADLE PRO 🇨🇴</h1>
        <h2>AVENTURA POR NIVELES</h2>
    </div>

    <div class="level-info">
        <h3>NIVEL <span id="currentLevel">1</span> / <span id="totalLevels">50</span></h3>
        <div class="progress-bar">
            <div class="progress-fill" id="progressFill"></div>
        </div>
        <div class="achievements" id="achievements">
            <div class="achievement" title="Primera Victoria">🏆</div>
            <div class="achievement" title="5 Niveles">⭐</div>
            <div class="achievement" title="10 Niveles">💎</div>
            <div class="achievement" title="Maestro de Jergas">🎯</div>
            <div class="achievement" title="Explorador Cultural">🗺️</div>
        </div>
    </div>

    <div class="game-container">
        <div class="hint-container">
            <strong>Pista:</strong> <span id="hint">¡Bienvenido al modo aventura!</span>
            <br>
            <span class="region-badge" id="regionBadge">COLOMBIA</span>
            <span id="category">🎮 JUEGO</span>
        </div>

        <div class="word-grid" id="wordGrid">
            <!-- Las filas se generarán dinámicamente -->
        </div>

        <div class="keyboard" id="keyboard">
            <!-- El teclado se generará dinámicamente -->
        </div>

        <div class="controls">
            <button class="btn btn-warning" onclick="showHint()">💡 Pista Extra</button>
            <button class="btn btn-primary" onclick="resetLevel()">🔄 Reiniciar</button>
        </div>
    </div>

    <!-- Modal de Victoria -->
    <div id="victoryModal" class="modal">
        <div class="modal-content">
            <div class="level-complete">
                <h2>🎉 ¡NIVEL COMPLETADO! 🎉</h2>
                <div class="stars" id="starsEarned">⭐⭐⭐</div>
                <p><strong>Palabra:</strong> <span id="completedWord"></span></p>
                <p><strong>Significado:</strong> <span id="wordMeaning"></span></p>
                <p><strong>Intentos:</strong> <span id="attemptsUsed"></span>/6</p>
            </div>
            
            <div class="next-level-preview" id="nextLevelInfo">
                <h3>🚀 PRÓXIMO NIVEL</h3>
                <p>Nivel <span id="nextLevel"></span>: <span id="nextRegion"></span></p>
                <p><em id="nextHint"></em></p>
            </div>
            
            <div class="controls">
                <button class="btn btn-success" onclick="nextLevel()">➡️ Siguiente Nivel</button>
                <button class="btn btn-primary" onclick="showLevelSelect()">📋 Seleccionar Nivel</button>
            </div>
        </div>
    </div>

    <!-- Modal de Derrota -->
    <div id="defeatModal" class="modal">
        <div class="modal-content">
            <h2>😔 ¡No lograste adivinarlo!</h2>
            <p><strong>La palabra era:</strong> <span id="revealedWord"></span></p>
            <p><strong>Significado:</strong> <span id="revealedMeaning"></span></p>
            <div class="controls">
                <button class="btn btn-warning" onclick="retryLevel()">🔄 Intentar de Nuevo</button>
                <button class="btn btn-primary" onclick="skipLevel()">⏭️ Saltar Nivel</button>
            </div>
        </div>
    </div>

    <script>
        // Base de datos de palabras por niveles (50 niveles progresivos)
        const levelWords = [
            // Niveles 1-10: Fácil
            { word: "AREPA", hint: "Pan de maíz típico colombiano", category: "COMIDA", region: "paisa", meaning: "Alimento básico hecho de masa de maíz" },
            { word: "PARCE", hint: "Amigo en jerga paisa", category: "JERGA", region: "paisa", meaning: "Forma cariñosa de decir amigo" },
            { word: "CUMBIA", hint: "Ritmo musical del Caribe", category: "MÚSICA", region: "costeno", meaning: "Género musical tradicional de la costa" },
            { word: "TINTO", hint: "Café negro en Bogotá", category: "BEBIDA", region: "bogotano", meaning: "Café sin leche ni azúcar" },
            { word: "SALSA", hint: "Baile típico de Cali", category: "MÚSICA", region: "caleno", meaning: "Género musical y baile de origen caribeño" },
            { word: "NIÑO", hint: "Pequeño ser humano", category: "FAMILIA", region: "general", meaning: "Persona en edad infantil" },
            { word: "FRIJOL", hint: "Legumbre de la bandeja paisa", category: "COMIDA", region: "paisa", meaning: "Leguminosa básica en la alimentación" },
            { word: "BOLLO", hint: "Masa de maíz costeña", category: "COMIDA", region: "costeno", meaning: "Alimento de maíz envuelto en hoja" },
            { word: "AJIACO", hint: "Sopa bogotana con papas", category: "COMIDA", region: "bogotano", meaning: "Sopa típica con pollo y diferentes papas" },
            { word: "LULADA", hint: "Bebida caleña refrescante", category: "BEBIDA", region: "caleno", meaning: "Bebida hecha con lulo y hielo" },
            
            // Niveles 11-20: Intermedio
            { word: "QUIUBO", hint: "Saludo paisa muy común", category: "JERGA", region: "paisa", meaning: "Contracción de '¿qué hubo?'" },
            { word: "VALLENATO", hint: "Música típica costeña", category: "MÚSICA", region: "costeno", meaning: "Género musical del Cesar y La Guajira" },
            { word: "CHANGUA", hint: "Sopa de leche con huevo", category: "COMIDA", region: "bogotano", meaning: "Sopa tradicional del altiplano" },
            { word: "CHAMPÚS", hint: "Bebida dulce valluna", category: "BEBIDA", region: "caleno", meaning: "Bebida tradicional con maíz y frutas" },
            { word: "BANDEJA", hint: "Plato típico antioqueño", category: "COMIDA", region: "paisa", meaning: "Plato con frijoles, arroz, carne y más" },
            { word: "SANCOCHO", hint: "Sopa costeña nutritiva", category: "COMIDA", region: "costeno", meaning: "Sopa con carnes, verduras y tubérculos" },
            { word: "MAZORCA", hint: "Maíz hervido bogotano", category: "COMIDA", region: "bogotano", meaning: "Maíz tierno cocido con sal" },
            { word: "EMPANADA", hint: "Masa frita con relleno", category: "COMIDA", region: "caleno", meaning: "Masa rellena de carne o pollo" },
            { word: "CHIMBA", hint: "Algo excelente en paisa", category: "JERGA", region: "paisa", meaning: "Expresión para algo muy bueno" },
            { word: "CHEVERE", hint: "Muy bueno en costeño", category: "JERGA", region: "costeno", meaning: "Algo agradable o positivo" },
            
            // Niveles 21-30: Intermedio-Avanzado
            { word: "BERRACO", hint: "Valiente o difícil paisa", category: "JERGA", region: "paisa", meaning: "Persona valiente o situación complicada" },
            { word: "MAMAGALLO", hint: "Algo increíble costeño", category: "JERGA", region: "costeno", meaning: "Situación extraordinaria" },
            { word: "BACANO", hint: "Genial en bogotano", category: "JERGA", region: "bogotano", meaning: "Algo muy bueno o agradable" },
            { word: "VACANO", hint: "Excelente en caleño", category: "JERGA", region: "caleno", meaning: "Algo de muy buena calidad" },
            { word: "AGUARDIENTE", hint: "Licor anisado nacional", category: "BEBIDA", region: "general", meaning: "Bebida alcohólica típica de Colombia" },
            { word: "CHICHARRÓN", hint: "Carne de cerdo frita", category: "COMIDA", region: "paisa", meaning: "Piel de cerdo frita hasta quedar crujiente" },
            { word: "PATACONES", hint: "Plátano verde aplastado", category: "COMIDA", region: "costeno", meaning: "Plátano verde frito y prensado" },
            { word: "TAMALES", hint: "Masa rellena en hoja", category: "COMIDA", region: "bogotano", meaning: "Masa de maíz rellena envuelta en hoja" },
            { word: "PANDEBONO", hint: "Pan de yuca valluno", category: "COMIDA", region: "caleno", meaning: "Pan hecho con almidón de yuca y queso" },
            { word: "GONORREA", hint: "Expresión fuerte paisa", category: "JERGA", region: "paisa", meaning: "Palabra para expresar molestia" },
            
            // Niveles 31-40: Avanzado
            { word: "ABORRAJADO", hint: "Plátano con queso frito", category: "COMIDA", region: "caleno", meaning: "Plátano maduro relleno de queso y frito" },
            { word: "BOCACHICO", hint: "Pez del río Magdalena", category: "FAUNA", region: "costeno", meaning: "Pez típico de los ríos colombianos" },
            { word: "ALMOJÁBANA", hint: "Pan de queso blanco", category: "COMIDA", region: "bogotano", meaning: "Pan hecho con cuajada y harina" },
            { word: "MANJARBLANCO", hint: "Dulce cremoso valluno", category: "POSTRE", region: "caleno", meaning: "Dulce tradicional del Valle del Cauca" },
            { word: "GUARAPO", hint: "Bebida de caña fermentada", category: "BEBIDA", region: "paisa", meaning: "Bebida alcohólica de caña de azúcar" },
            { word: "MASATO", hint: "Bebida de arroz fermentada", category: "BEBIDA", region: "costeno", meaning: "Bebida tradicional hecha con arroz" },
            { word: "MAZAMORRA", hint: "Postre cremoso de maíz", category: "POSTRE", region: "bogotano", meaning: "Postre hecho con maíz pelado y leche" },
            { word: "AREQUIPE", hint: "Dulce de leche colombiano", category: "POSTRE", region: "general", meaning: "Dulce hecho con leche y azúcar" },
            { word: "FRITANGA", hint: "Variedad de comida frita", category: "COMIDA", region: "bogotano", meaning: "Conjunto de diferentes frituras" },
            { word: "MONDONGO", hint: "Sopa de callos costeña", category: "COMIDA", region: "costeno", meaning: "Sopa hecha con estómago de res" },
            
            // Niveles 41-50: Experto
            { word: "MAMAGALLISMO", hint: "Algo increíblemente bueno", category: "JERGA", region: "costeno", meaning: "Expresión para algo extraordinario" },
            { word: "ENVERRACADO", hint: "Muy enojado en paisa", category: "JERGA", region: "paisa", meaning: "Estado de mucha ira o molestia" },
            { word: "GUACHAFITA", hint: "Desorden o alboroto", category: "JERGA", region: "costeno", meaning: "Situación de caos o desorden" },
            { word: "TRABAJADERO", hint: "Lugar de mucho trabajo", category: "JERGA", region: "bogotano", meaning: "Sitio donde se trabaja intensamente" },
            { word: "ENCALETARSE", hint: "Esconderse en caleño", category: "JERGA", region: "caleno", meaning: "Refugiarse o esconderse" },
            { word: "TARRAYA", hint: "Red circular de pesca", category: "HERRAMIENTA", region: "costeno", meaning: "Red de pescar que se lanza al agua" },
            { word: "CHINCHORRO", hint: "Hamaca guajira", category: "OBJETO", region: "costeno", meaning: "Hamaca típica de La Guajira" },
            { word: "CONTRADANZA", hint: "Baile tradicional andino", category: "MÚSICA", region: "bogotano", meaning: "Danza folclórica del interior" },
            { word: "CURRULAO", hint: "Ritmo del Pacífico", category: "MÚSICA", region: "caleno", meaning: "Género musical afrocolombiano" },
            { word: "MARIMONDA", hint: "Personaje del carnaval", category: "CULTURA", region: "costeno", meaning: "Disfraz típico del Carnaval de Barranquilla" }
        ];

        let currentLevelIndex = 0;
        let currentRow = 0;
        let currentWord = '';
        let gameBoard = [];
        let gameActive = true;
        let keyboardState = {};
        let unlockedLevels = parseInt(localStorage.getItem('arepadleUnlockedLevels') || '1');
        let completedLevels = JSON.parse(localStorage.getItem('arepadleCompletedLevels') || '[]');

        const maxAttempts = 6;
        const alphabet = 'ABCDEFGHIJKLMNÑOPQRSTUVWXYZ'.split('');

        function initGame() {
            updateLevelInfo();
            setupLevel();
            createKeyboard();
            updateAchievements();
        }

        function updateLevelInfo() {
            document.getElementById('currentLevel').textContent = currentLevelIndex + 1;
            document.getElementById('totalLevels').textContent = levelWords.length;
            
            const progress = ((currentLevelIndex) / levelWords.length) * 100;
            document.getElementById('progressFill').style.width = progress + '%';
        }

        function setupLevel() {
            const level = levelWords[currentLevelIndex];
            currentWord = level.word;
            
            // Actualizar información del nivel
            document.getElementById('hint').textContent = level.hint;
            document.getElementById('category').textContent = `📍 ${level.category}`;
            
            const badge = document.getElementById('regionBadge');
            badge.textContent = level.region.toUpperCase();
            badge.className = `region-badge ${level.region}`;
            
            // Resetear estado del juego
            currentRow = 0;
            gameBoard = [];
            gameActive = true;
            keyboardState = {};
            
            // Crear grid de palabras
            createWordGrid();
            updateKeyboard();
        }

        function createWordGrid() {
            const grid = document.getElementById('wordGrid');
            grid.innerHTML = '';
            
            // Reinicializar gameBoard
            gameBoard = [];
            
            for (let i = 0; i < maxAttempts; i++) {
                const row = document.createElement('div');
                row.className = 'word-row';
                
                const rowData = [];
                
                for (let j = 0; j < currentWord.length; j++) {
                    const box = document.createElement('div');
                    box.className = 'letter-box';
                    box.id = `box-${i}-${j}`;
                    box.textContent = '';
                    row.appendChild(box);
                    rowData.push('');
                }
                
                grid.appendChild(row);
                gameBoard.push(rowData);
            }
            
            console.log(`Grid creado para palabra: ${currentWord} (${currentWord.length} letras)`);
            console.log(`GameBoard inicializado:`, gameBoard);
        }

        function createKeyboard() {
            const keyboard = document.getElementById('keyboard');
            keyboard.innerHTML = '';
            
            // Primera fila del teclado
            const row1 = 'QWERTYUIOP'.split('');
            // Segunda fila del teclado
            const row2 = 'ASDFGHJKLÑ'.split('');
            // Tercera fila del teclado
            const row3 = 'ZXCVBNM'.split('');
            
            // Crear primera fila
            row1.forEach(letter => {
                const key = document.createElement('button');
                key.className = 'key';
                key.textContent = letter;
                key.onclick = () => addLetter(letter);
                keyboard.appendChild(key);
            });
            
            // Crear segunda fila
            row2.forEach(letter => {
                const key = document.createElement('button');
                key.className = 'key';
                key.textContent = letter;
                key.onclick = () => addLetter(letter);
                keyboard.appendChild(key);
            });
            
            // Crear tercera fila con botones especiales
            const enterKey = document.createElement('button');
            enterKey.className = 'key enter';
            enterKey.textContent = 'ENTER';
            enterKey.onclick = submitGuess;
            keyboard.appendChild(enterKey);
            
            row3.forEach(letter => {
                const key = document.createElement('button');
                key.className = 'key';
                key.textContent = letter;
                key.onclick = () => addLetter(letter);
                keyboard.appendChild(key);
            });
            
            const backspaceKey = document.createElement('button');
            backspaceKey.className = 'key backspace';
            backspaceKey.textContent = '⌫';
            backspaceKey.onclick = deleteLetter;
            keyboard.appendChild(backspaceKey);
        }

        function addLetter(letter) {
            if (!gameActive) return;
            
            const currentRowData = gameBoard[currentRow];
            if (!currentRowData) {
                console.log('No hay fila actual disponible');
                return;
            }
            
            const emptyIndex = currentRowData.indexOf('');
            
            if (emptyIndex !== -1 && emptyIndex < currentWord.length) {
                currentRowData[emptyIndex] = letter;
                const box = document.getElementById(`box-${currentRow}-${emptyIndex}`);
                if (box) {
                    box.textContent = letter;
                    box.classList.add('filled');
                    box.style.transform = 'scale(1.1)';
                    box.style.borderColor = 'rgba(255, 255, 255, 0.8)';
                    setTimeout(() => {
                        box.style.transform = 'scale(1)';
                    }, 150);
                    console.log(`Letra ${letter} añadida en posición ${emptyIndex}`);
                } else {
                    console.log(`No se encontró la casilla box-${currentRow}-${emptyIndex}`);
                }
            } else {
                console.log(`No se puede añadir más letras. EmptyIndex: ${emptyIndex}, Longitud palabra: ${currentWord.length}`);
            }
        }

        function deleteLetter() {
            if (!gameActive) return;
            
            const currentRowData = gameBoard[currentRow];
            if (!currentRowData) return;
            
            for (let i = currentRowData.length - 1; i >= 0; i--) {
                if (currentRowData[i] !== '') {
                    currentRowData[i] = '';
                    const box = document.getElementById(`box-${currentRow}-${i}`);
                    if (box) {
                        box.textContent = '';
                        box.classList.remove('filled');
                        box.style.transform = 'scale(0.9)';
                        setTimeout(() => {
                            box.style.transform = 'scale(1)';
                        }, 150);
                        console.log(`Letra borrada de posición ${i}`);
                    }
                    break;
                }
            }
        }

        function submitGuess() {
            if (!gameActive) return;
            
            const currentRowData = gameBoard[currentRow];
            
            // Verificar que la fila esté completa
            if (currentRowData.includes('')) {
                showMessage('¡Completa la palabra!', 'warning');
                return;
            }
            
            const guess = currentRowData.join('');
            checkWord(guess);
        }

        function checkWord(guess) {
            const targetWord = currentWord;
            const result = [];
            const targetLetters = targetWord.split('');
            const guessLetters = guess.split('');
            
            // Marcar letras correctas primero
            for (let i = 0; i < guessLetters.length; i++) {
                if (guessLetters[i] === targetLetters[i]) {
                    result[i] = 'correct';
                    targetLetters[i] = null;
                    guessLetters[i] = null;
                }
            }
            
            // Marcar letras parciales
            for (let i = 0; i < guessLetters.length; i++) {
                if (guessLetters[i] !== null) {
                    const targetIndex = targetLetters.indexOf(guessLetters[i]);
                    if (targetIndex !== -1) {
                        result[i] = 'partial';
                        targetLetters[targetIndex] = null;
                    } else {
                        result[i] = 'incorrect';
                    }
                }
            }
            
            // Animar y colorear las casillas
            animateRow(currentRow, result);
            
            // Actualizar teclado
            updateKeyboardState(guess, result);
            
            // Verificar victoria
            if (guess === targetWord) {
                gameActive = false;
                setTimeout(() => {
                    levelComplete(currentRow + 1);
                }, 1500);
                return;
            }
            
            // Verificar derrota
            currentRow++;
            if (currentRow >= maxAttempts) {
                gameActive = false;
                setTimeout(() => {
                    levelFailed();
                }, 1500);
                return;
            }
        }

        function animateRow(row, result) {
            for (let i = 0; i < result.length; i++) {
                setTimeout(() => {
                    const box = document.getElementById(`box-${row}-${i}`);
                    box.className = `letter-box ${result[i]}`;
                }, i * 200);
            }
        }

        function updateKeyboardState(guess, result) {
            for (let i = 0; i < guess.length; i++) {
                const letter = guess[i];
                const state = result[i];
                
                if (!keyboardState[letter] || 
                    (keyboardState[letter] !== 'correct' && state === 'correct') ||
                    (keyboardState[letter] === 'incorrect' && state === 'partial')) {
                    keyboardState[letter] = state;
                }
            }
            
            updateKeyboard();
        }

        function updateKeyboard() {
            const keys = document.querySelectorAll('.key');
            keys.forEach(key => {
                const letter = key.textContent;
                if (keyboardState[letter]) {
                    key.className = `key ${keyboardState[letter]}`;
                }
            });
        }

        function levelComplete(attempts) {
            const level = levelWords[currentLevelIndex];
            const stars = attempts <= 2 ? 3 : attempts <= 4 ? 2 : 1;
            
            // Actualizar progreso
            if (!completedLevels.includes(currentLevelIndex + 1)) {
                completedLevels.push(currentLevelIndex + 1);
                localStorage.setItem('arepadleCompletedLevels', JSON.stringify(completedLevels));
            }
            
            if (currentLevelIndex + 1 >= unlockedLevels) {
                unlockedLevels = Math.min(currentLevelIndex + 2, levelWords.length);
                localStorage.setItem('arepadleUnlockedLevels', unlockedLevels.toString());
            }
            
            // Mostrar modal de victoria
            document.getElementById('completedWord').textContent = level.word;
            document.getElementById('wordMeaning').textContent = level.meaning;
            document.getElementById('attemptsUsed').textContent = attempts;
            document.getElementById('starsEarned').textContent = '⭐'.repeat(stars);
            
            // Información del próximo nivel
            if (currentLevelIndex + 1 < levelWords.length) {
                const nextLevelData = levelWords[currentLevelIndex + 1];
                document.getElementById('nextLevel').textContent = currentLevelIndex + 2;
                document.getElementById('nextRegion').textContent = nextLevelData.region.toUpperCase();
                document.getElementById('nextHint').textContent = nextLevelData.hint;
                document.getElementById('nextLevelInfo').style.display = 'block';
            } else {
                document.getElementById('nextLevelInfo').style.display = 'none';
            }
            
            updateAchievements();
            document.getElementById('victoryModal').style.display = 'block';
        }

        function levelFailed() {
            const level = levelWords[currentLevelIndex];
            document.getElementById('revealedWord').textContent = level.word;
            document.getElementById('revealedMeaning').textContent = level.meaning;
            document.getElementById('defeatModal').style.display = 'block';
        }

        function nextLevel() {
            if (currentLevelIndex + 1 < levelWords.length) {
                currentLevelIndex++;
                document.getElementById('victoryModal').style.display = 'none';
                updateLevelInfo();
                setupLevel();
            } else {
                showMessage('¡Felicidades! Has completado todos los niveles', 'success');
            }
        }

        function retryLevel() {
            document.getElementById('defeatModal').style.display = 'none';
            setupLevel();
        }

        function skipLevel() {
            if (currentLevelIndex + 1 < levelWords.length) {
                currentLevelIndex++;
                document.getElementById('defeatModal').style.display = 'none';
                updateLevelInfo();
                setupLevel();
            }
        }

        function resetLevel() {
            setupLevel();
        }

        function showHint() {
            const level = levelWords[currentLevelIndex];
            const extraHints = [
                `Región: ${level.region}`,
                `Categoría: ${level.category}`,
                `Letras: ${level.word.length}`,
                `Primera letra: ${level.word[0]}`,
                `Última letra: ${level.word[level.word.length - 1]}`
            ];
            
            const randomHint = extraHints[Math.floor(Math.random() * extraHints.length)];
            showMessage(`💡 Pista extra: ${randomHint}`, 'info');
        }

        function updateAchievements() {
            const achievements = document.querySelectorAll('.achievement');
            const completed = completedLevels.length;
            
            // Primera Victoria
            if (completed >= 1) {
                achievements[0].classList.add('unlocked');
            }
            
            // 5 Niveles
            if (completed >= 5) {
                achievements[1].classList.add('unlocked');
            }
            
            // 10 Niveles
            if (completed >= 10) {
                achievements[2].classList.add('unlocked');
            }
            
            // Maestro de Jergas (completar niveles de jerga)
            const jergaLevels = levelWords.filter((level, index) => 
                level.category === 'JERGA' && completedLevels.includes(index + 1)
            ).length;
            if (jergaLevels >= 5) {
                achievements[3].classList.add('unlocked');
            }
            
            // Explorador Cultural (completar niveles de todas las regiones)
            const regions = ['paisa', 'costeno', 'bogotano', 'caleno'];
            const completedRegions = regions.filter(region => 
                levelWords.some((level, index) => 
                    level.region === region && completedLevels.includes(index + 1)
                )
            );
            if (completedRegions.length >= 4) {
                achievements[4].classList.add('unlocked');
            }
        }

        function showLevelSelect() {
            // Esta función se puede expandir para mostrar un selector de niveles
            showMessage('Función de selección de niveles próximamente', 'info');
            document.getElementById('victoryModal').style.display = 'none';
        }

        function showMessage(message, type) {
            // Crear y mostrar mensaje temporal
            const messageDiv = document.createElement('div');
            messageDiv.style.cssText = `
                position: fixed;
                top: 20px;
                left: 50%;
                transform: translateX(-50%);
                padding: 15px 25px;
                border-radius: 25px;
                color: white;
                font-weight: bold;
                z-index: 1001;
                opacity: 0;
                transition: all 0.3s ease;
            `;
            
            switch(type) {
                case 'success':
                    messageDiv.style.background = 'linear-gradient(45deg, #2ecc71, #27ae60)';
                    break;
                case 'warning':
                    messageDiv.style.background = 'linear-gradient(45deg, #f39c12, #e67e22)';
                    break;
                case 'info':
                    messageDiv.style.background = 'linear-gradient(45deg, #3498db, #2980b9)';
                    break;
                default:
                    messageDiv.style.background = 'linear-gradient(45deg, #667eea, #764ba2)';
            }
            
            messageDiv.textContent = message;
            document.body.appendChild(messageDiv);
            
            // Animar entrada
            setTimeout(() => {
                messageDiv.style.opacity = '1';
                messageDiv.style.transform = 'translateX(-50%) translateY(10px)';
            }, 100);
            
            // Remover después de 3 segundos
            setTimeout(() => {
                messageDiv.style.opacity = '0';
                messageDiv.style.transform = 'translateX(-50%) translateY(-10px)';
                setTimeout(() => {
                    document.body.removeChild(messageDiv);
                }, 300);
            }, 3000);
        }

        // Event listeners para teclado físico
        document.addEventListener('keydown', (e) => {
            // Solo procesar si no estamos en un input o textarea
            if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA') {
                return;
            }
            
            if (!gameActive) return;
            
            const key = e.key.toUpperCase();
            
            if (alphabet.includes(key)) {
                e.preventDefault();
                addLetter(key);
            } else if (key === 'ENTER') {
                e.preventDefault();
                submitGuess();
            } else if (key === 'BACKSPACE') {
                e.preventDefault();
                deleteLetter();
            }
        });

        // Cerrar modales al hacer clic fuera
        window.onclick = function(event) {
            const victoryModal = document.getElementById('victoryModal');
            const defeatModal = document.getElementById('defeatModal');
            
            if (event.target === victoryModal) {
                victoryModal.style.display = 'none';
            }
            if (event.target === defeatModal) {
                defeatModal.style.display = 'none';
            }
        }

        // Inicializar el juego
        initGame();
    </script>
</body>
</html>
